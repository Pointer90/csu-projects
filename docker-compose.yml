version: '3.11'

services:
  django_container:
    build:
      dockerfile: Dockerfile
      context: .

    image: csu_prj:latest
    container_name: django_container
    command: python ./Django/manage.py runserver 0.0.0.0:8000
    ports:
      - 8000:8000